# Linking one container to another  

From the previous chapters, we should have a basic understanding of how Docker daemon interacts with Docker cli and some of the most useful commands. In this chapter, we will be linking one container to another, which has an important significance in managing microservices (as discussed in earlier chapters) using Docker. Let's get straight into it.  

### 
